# 金融长文档问答 Agent —— 方案设计文档

> 赛题：天池/阿里云「金融长文档问答」算法大赛
> 评分：`FinalScore = 100 * Accuracy * (0.7 + 0.3 * TokenScore)`，`TokenScore = max(0, min(1, (5,000,000 - TotalTokens) / 5,000,000))`
> 硬约束：推理问答阶段只能用 **Qwen 系列模型 API**（百炼/魔搭），**禁用任何 embedding 模型**；解析阶段可用非 Qwen 工具但产物不得直接参与答题。
> 基准模型：**Qwen3.6-plus**。

---

## 1. 赛题理解与目标拆解

### 1.1 一句话目标
在 **500 万 token 预算**内，对五个金融领域的长文档选择题（单选/多选/判断）做出尽可能准确的回答，并产出可追溯的证据。

### 1.2 评分公式的策略含义
- 准确率权重占 70%，是绝对主导项；token 效率最多影响 30% 的系数。
- 关键推论：**先保准确率，再省 token**。但 baseline 已消耗 687 万 token（爆预算），说明朴素全文输入会让 TokenScore 直接归零。
- 目标定位：把单题平均 token 控制在 **2~3 万以内**（100 题约 200~300 万），既留出 TokenScore 余量，又给高准确率所需的多轮推理留空间。

| 场景 | Accuracy | TotalTokens | TokenScore | FinalScore |
|---|---|---|---|---|
| Baseline A组 | 0.49 | 2.99M | 0.40 | 40.2 |
| 目标态 | 0.75 | 2.5M | 0.50 | 63.75 |
| 高准确率态 | 0.85 | 3.0M | 0.40 | 67.2 |

> 结论：准确率每提升 10% 的收益远大于把 token 砍半，但 token 不能爆预算。

### 1.3 两个榜单的差异
- **A 榜（当前数据）**：题目直接给 `doc_ids`，无需全库检索，重点打磨「解析 + 段内检索 + 推理」。
- **B 榜**：不给 `doc_ids`，必须先做**全库候选文档召回**再答题。检索模块必须从一开始就按 B 榜设计（A 榜只是把候选集缩小到已知 doc_ids）。

---

## 2. 数据集现状

### 2.1 题目
`questions/group_a/<domain>_questions.json`，5 个领域各 20 题，共 100 题（A 榜）。字段：
`qid / domain / split / question / options / answer_format(mcq|multi|tf) / type / doc_ids`

### 2.2 文档（`raw/<domain>/`）
| 领域 | 文档数 | 文件形态 | doc_id → 文件映射 |
|---|---|---|---|
| insurance | 16 | `1.pdf`…`16.pdf` | doc_id `"1"` → `1.pdf` |
| regulatory | 26（含 513 个 txt/html/附件碎片） | txt + html + attachments | `strict_v3_xxx` / `strict_csrc_xxx` |
| financial_contracts | 14 | `text01.pdf`… | `text01` → `text01.pdf` |
| financial_reports | 10 | `annual_xxx_report.PDF` | doc_id 即文件名去扩展名 |
| research | 20 | `pack2_textNN.pdf` | 同上 |

> 注意：`doc_id = 文件名（去扩展名）`。需要建立一张 `doc_id ↔ path ↔ title` 的映射表（赛题 3.4 提到的「文档元数据」当前未随数据提供，需自建）。

---

## 3. 系统架构

```
                 ┌─────────────────────────────────────────────┐
                 │            离线预处理（非 Qwen 允许）          │
PDF/HTML/TXT ──▶ │  解析 → 版面/表格还原 → 切块 → 结构化索引      │ ──▶ processed_data/
                 └─────────────────────────────────────────────┘
                                      │
  question ──────────────────────────┼───────────────────────────────────────┐
                                      ▼                                         │
                 ┌──────────────────────────────┐   ┌──────────────────────┐  │
                 │  检索（BM25 + 金融术语规则）   │──▶│  证据压缩 / 记忆管理   │  │
                 │  A:用doc_ids  B:全库召回+精排  │   │  抽取式为主，Qwen为辅 │  │
                 └──────────────────────────────┘   └──────────────────────┘  │
                                      │                                         │
                                      ▼                                         │
                 ┌──────────────────────────────┐                              │
                 │   Qwen3.6-plus 推理（分领域）  │◀─────────────────────────────┘
                 │   结构化输出 answer + evidence │
                 └──────────────────────────────┘
                                      │
                                      ▼
                 ┌──────────────────────────────┐
                 │  答案后处理 + Token 统计汇总   │ ──▶ answer.csv / evidence.json
                 └──────────────────────────────┘
```

### 目录结构（对齐提交要求 submission.zip）
```
agent/            # Agent 系统代码（检索/压缩/推理/后处理）
script/           # 可复现脚本：解析、建索引、检索、推理、生成结果
processed_data/   # 解析切分后的结构化长文本
logs/             # 每题调用日志、token 明细、证据
config/           # 模型名、API、预算、检索参数
answer.csv
evidence.json
requirements.txt
README.md
```

---

## 4. 模块设计

### 4.1 Token 统计中间件（最先实现，评分必需）
- 所有 Qwen 调用统一经过一个 `qwen_client.chat()` 封装，自动累加 `prompt_tokens / completion_tokens`。
- 按 `qid` 分桶记录，便于定位 token 大户；最终汇总写入 `answer.csv` 的 `summary` 行。
- 覆盖范围：检索摘要、上下文压缩、证据判断、答案生成、自检——**所有 API 调用都要计入**（赛题明确要求）。

### 4.2 文档解析（阶段允许非 Qwen）
- PDF：优先 MinerU / pdfplumber；**表格用 camelot/表格还原**（财报、保险条款大量依赖表格数值）。
- HTML（regulatory）：用解析器抽正文，去导航/脚本；附件单独处理。
- 产出 `processed_data/<doc_id>.jsonl`：每条为一个 chunk，保留
  `{doc_id, chunk_id, section_title, clause_no, page, text, is_table}`。
- 切块策略：**按结构（章/条/表）+ 重叠滑窗**，避免切断条款；财报按报表科目切。

### 4.3 检索（禁用 embedding → 词面 + 规则混合）
- 基础召回：**BM25**（jieba/金融词典分词），对以下要素加权：
  - 条款编号（第 X 条/第 X 款）、指标名（营业收入、净利润、现金价值、研发投入）、产品名、法规名、日期/时限、比例/金额。
- Query 处理：用 Qwen 对题干做**关键词/实体抽取与改写**（少量 token），生成检索词（这是允许的，因为用的是 Qwen）。
- A 榜：检索范围限定在题目给的 `doc_ids` 内 → 段落级召回。
- B 榜：全库 BM25 召回 Top-K 文档 → 文档内段落精排 → 再答题。
- 精排：用规则打分或 Qwen 轻量判断段落相关性（控制 token）。

### 4.4 证据压缩 / 记忆管理
- 先**抽取式裁剪**（规则保留命中段落 + 邻近上下文），把无关内容剔除。
- 跨文档对比题（财报年度对比、保险产品比较）：分文档抽取关键指标，组装成**对齐小表**喂模型，避免重复长文本。
- 多轮记忆：同一题多文档时维护一个紧凑的「证据备忘」，避免重复把全文塞回。

### 4.5 Qwen 推理（分领域 Prompt）
| 领域 | Prompt 重点 |
|---|---|
| insurance | 条款触发条件 + 计算公式（身故保险金、退保金额、账户价值） |
| regulatory | 法条追溯、施行日期、报告时限、表决/管控要求；严格依据条文 |
| financial_reports | 指标提取与同比对比、现金流、分红比例、研发占比 |
| financial_contracts | 债券条款、发行/评级信息、权利义务关系 |
| research | 行业趋势、公司比较、结论核验 |

- **结构化输出**：要求模型返回 JSON `{answer, evidence:[{doc_id, clause/page, quote}], reasoning(简短)}`。
- 控长：CoT 但限制 `max_tokens`，多选/判断题尤其压缩 completion。
- 自检（可选、按 token 余量开启）：对低置信题做一次复核。

### 4.6 答案后处理（保证格式完全匹配）
- 从模型输出抽取大写字母 → 去重 → 排序（多选无分隔符，如 `ABC`）。
- 单选/判断取首个有效字母。
- 合法性校验：空答案/非法字符/越界选项 → 记错并打日志。
- 生成 `answer.csv`：首行 `summary` 含 token 统计，其余每行 `qid, answer, prompt_tokens, completion_tokens, total_tokens`。

---

## 5. Token 预算分配（初步）
| 环节 | 单题预估 token | 说明 |
|---|---|---|
| Query 关键词抽取 | ~0.5K | 题干较短 |
| 检索/段落精排（可选用 Qwen） | ~3~6K | B 榜更高，尽量用规则替代 |
| 证据压缩 | ~2~4K | 抽取式为主，少调模型 |
| 主推理 | ~8~15K | 主要开销 |
| 自检（按需） | ~5K | 仅低置信题 |
| **单题合计目标** | **< 25K** | 100 题 ≈ 2.5M，预算 5M 充裕 |

---

## 6. 里程碑路线图
1. **M0 脚手架**：目录、config、Qwen 封装 + token 中间件、doc_id↔path 映射。
2. **M1 解析**：5 个领域 PDF/HTML → `processed_data` 结构化 chunk（含表格）。
3. **M2 检索**：BM25 + 金融术语加权；A 榜用 doc_ids，验证段落召回质量。
4. **M3 推理 MVP**：先打通 financial_reports 一个领域端到端，产出 answer.csv。
5. **M4 全领域 + 分领域 Prompt**：覆盖 100 题，记录 token 与可追溯证据。
6. **M5 调优**：错题分析、检索/压缩调参、token 压缩，逼近目标分。
7. **M6 B 榜适配**：全库检索召回，去掉对 doc_ids 的依赖。
8. **M7 提交打包**：evidence.json、复现脚本、README、requirements，验证一键运行、≤1GB。

---

## 7. 风险与对策
| 风险 | 对策 |
|---|---|
| 禁用 embedding，纯词面检索召回不足 | 金融术语词典 + 条款号/指标名加权 + Qwen 做 query 扩展 |
| PDF 表格解析失真导致数值错误 | 专用表格还原 + 关键数值二次校验 |
| 多选题无部分分，容错低 | 强制证据可追溯 + 低置信题自检复核 |
| Token 爆预算 | 抽取式压缩优先、限制 max_tokens、监控单题 token 大户 |
| B 榜无 doc_ids | 检索模块从一开始按全库召回设计 |
| A 榜无标准答案做验证 | 自建小规模人工校验集 + 与 baseline 对照 |
```

